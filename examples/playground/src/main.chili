// let { println } = import!("std/fmt")
let { printf } = import!("std/c")

let Node = struct {
    l: *mut Node,
    r: *mut Node,
}

let mut null_node: *mut Node = 0 as uint as _;

let node_check = fn(node: *Node) -> i32 {
    let mut ret = 1;
    let l = node.l;
    
    // if node.l != null_node {
        
    // }

    ret
}

let main = fn {
    let node = Node {
        l: null_node,
        r: null_node
    };
    // println("Hello World");
}