let ? = import!("std/build");

let build = fn() {
	add(5, 10);
	add_destructed(.{ a: 1, b: 3});
};

let add = fn(a: int, b: int) -> int { 
	a + b 
};

let add_destructed = fn({ a, b }: struct { a: int, b: int }) -> int { 
	a + b 
};