use c.{ printf }

let person = ("Ron", 26)
// let person = .{ name: "Ron", age: 26 }

let main = fn() {
    // printf("Hello\n".data)
    // let person = @run(get_value())
    printf("Name = %s, Age = %d\n".data, person.0.data, person.1)
    // printf("Name = %s, Age = %d\n".data, person.name.data, person.age)
    // printf("Goodbye\n".data)
    ()
}

// let get_value = fn() {
//     .{ name: "Ron", age: 26 }
// }