use c.printf
use raylib_game_of_life.main as game_of_life

let foo = {
    let x = 5
    x + 10
}

let main = fn() {
    // game_of_life returns the amounts of cells that were alive :)
    printf("foo = %d\n".data, foo)
}

@run(main())