use c.{ printf }

let main = fn() {
    printf("Hello\n".data)
    let (name, age) = @run(get_value())
    printf("Name = %s, Age = %d\n".data, name.data, age)
    printf("Goodbye\n".data)
}

let get_value = fn() -> (str, i64) {
    ("Ron", 26)
}