use c.{ printf }

let main = fn() {
    printf("Hello\n".data)
    let (name, age) = @run(get_value())
    printf("Name = %s, Age = %d %d\n".data, name.data, age, @run(get_value()).1 + 10)
    printf("Goodbye\n".data)
}

let get_value = fn() -> (str, i64) {
    ("Ron", 26)
}