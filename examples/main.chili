use c.?

let test = fn {
    // printf("Hello, Vm!\n".data)
    
    // let x = malloc(@size_of(int)) as *int
    // x.* = 42
    // printf("The answer is %d\n".data, x.*)
    // printf("My name is %s and I'm %d years old\n".data, "Ron".data, 26 as i64)

    let person = .{ name: "Ron", age: 26 as i64 }
    let age = person.age
    printf("My name is %s and I'm %d years old\n".data, person.name.data, age)
}

@run(test())