use c
use c.printf

let main = fn {
    let answer: i32 = 42
    print_answer(answer)
}

let print_answer = fn(answer) {
    c.printf("What is the answer?\n".data)
    printf("The answer is %d\n".data, answer)
}