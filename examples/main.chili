use std.{
    c.printf,
    fmt.println
};

let Vector2 = struct { x: int, y: int }
// let (x, y) = (1, 2);


let main = fn() {
    printf("Vector2(%d, %d)\n".data, x, y);
}

let { x, y } = Vector2 { x: 1, y: 2 };
