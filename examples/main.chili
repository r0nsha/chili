use c.printf
use raylib_game_of_life.main as game_of_life

let main = fn() {
    printf("cells alive = %d\n".data, @run(game_of_life()))
    // @run({
    //     let count = game_of_life()
    //     printf("cells alive = %d\n".data, count)
    // })
}