use std.{
    c.printf,
    fmt.println,
    // types
}

let A = struct {
    a: int,
    b: int
}

let main = fn() {
    let a: A = .{ a: 1, b: 2 };
    printf("%d %d\n".data, a.a, a.b);
}

@run(main())