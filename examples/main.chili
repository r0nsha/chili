use std.{
    c.printf,
    fmt.println
};

let Vector2 = struct { x: int, y: int }
// let { mut x, mut y } = Vector2 { x: 1, y: 2 };
let (mut x, mut y) = (3, 2);

let ff = fn() -> u64 { 42 }

let main = fn() {
    printf("Vector2(%d, %d)\n".data, x, y);
}

