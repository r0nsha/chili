let { printf } = @import("std/c");
let { println } = @import("std/fmt");

let main = fn() {
    println("Hello World");

    let { bar, baz } = @import("foo");
    printf("bar = %d, baz = %d\n".data, bar(), baz);
};