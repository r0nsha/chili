use c.{printf, malloc}

let build = fn {
    // let x = malloc(8) as *int;
    let mut x = 5
    printf("hello %d\n\0".data, x)
    x = 420
    printf("hello %d\n\0".data, x)
    // printf("hello %d\n\0".data, x)
    // printf("hello %d\n\0".data, x.*)
}

@run(build())