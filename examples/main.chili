let ? = @import("std/fmt");
// let { printf } = @import("std/c");

let extern printf: fn(input: [*]u8, ..args) -> int;

let main = fn() {
    println("Hello World");

    let array = .[1, 2, 3];

    for it, index in array {
        printf("%d = %d\n".data, index, it)
    }
};