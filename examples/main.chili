use c
use c.printf

let main = fn {
    let x: i32 = 42
    let y: int = x
    let z = x + y
    let v = -z + x
    let answer: i32 = 42
    print_answer(answer)
}

let print_answer = fn(answer) {
    c.printf("What is the answer?\n".data)
    printf("The answer is %d\n".data, answer)
}