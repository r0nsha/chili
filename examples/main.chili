use c.{ printf }

let main = fn() {
    printf("Hello\n".data)
    let person = @run(get_value())
    printf("Name = %s, Age = %d\n".data, person.name.data, person.age)
    printf("Goodbye\n".data)
}

type Person = { name: str, age: i64 }

let get_value = fn() {
    .{ name: "Ron", age: 26 }
}