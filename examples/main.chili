use c.printf

let main = fn {
	printf("%d\n".data, test(5)) // invalid
	let array = [1,2,3]
	// printf("%d\n".data, test(array)) // valid
	// printf("%d\n".data, test(array[..])) // valid
}

let test = fn(x) {
	x[0]
}