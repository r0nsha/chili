use c.{ printf }

let main = fn() {
    println("Hello");
    let x: i32 = foo();
    printf("x = %d\n".data, x);
    println("Goodbye");
    ()
}

let foo = fn() {
    5
}

let println = fn(fmt) {
    printf("%s\n".data, fmt)
}