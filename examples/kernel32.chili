
foreign("system:kernel32") {
    pub let GetStdHandle = fn(handle: i32) -> HANDLE
    pub let WriteFile = fn(
		status: i64, 
		buffer: [*]u8, 
		size: uint, 
		bytes_written: *mut i32, 
		overlapped: *i64
	) -> i64
}