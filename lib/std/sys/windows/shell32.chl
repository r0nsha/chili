use types.*

let shell32 = "shell32"

@lib(shell32)
pub extern fn ShellExecuteA(
    hwnd: HWND,
    lpOperation: LPCSTR,
    lpFile: LPCSTR,
    lpParameters: LPCSTR,
    lpDirectory: LPCSTR,
    nShowCmd: HINSTANCE,
) -> HINSTANCE